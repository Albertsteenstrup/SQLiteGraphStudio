# Agent exploration implementation status

Status as of 23 September 2026. This report records implementation and verification evidence for the [design](agent-exploration-design.md), local MCP surface, and [acceptance plan](agent-exploration-validation.md). It is not a release sign-off. No A01–A38 scenario is certified complete by this report.

## Implemented and exercised

- **Local coding-agent MCP.** The bundled `StudioMCP` stdio helper discovers 62 tools and forwards scoped requests to the running app over its private local bridge. A real Codex CLI invocation using `gpt-6-luna` discovered the tools and called `studio_status`; it reported `appRunning=true` and `bridgeConnected=true`. The native stdio test also passed. A scratch app interaction confirmed the graph/data split, tab switching, opening a table from the graph, and `studio_show_tables` narrowing the visible graph to the requested tables. Claude Code has not been exercised; its CLI is not installed on this host. See [MCP implementation](../Sources/StudioMCP/) and [automation coordinator](../Sources/SQLiteGraphStudio/StudioAutomationCoordinator.swift).
- **Agent-selected, non-story presentation.** The replacement `database-explore` skill tells the agent to choose the visual treatment and amount of guidance that fits the request. Tools can make an immediate view change or prepare and control short captioned presentation points; no fixed user-story sequence is required. The agent can show application-related tables near one another without drawing a database relationship between them. Generated Codex and Claude skill copies are checked against their canonical source. The old `story-flows` skill and story-specific UI/playback code have been removed.
- **Workspace, ownership, and recovery foundations.** Workspaces use split graph/data panes by default and keep browsing state per tab. MCP contexts bind to a task and workspace; source IDs and view revisions guard requests. Tab ownership, task-scoped listing, reconnect tokens and bounded idempotency receipts prevent one coding task from taking over another task's workspace implicitly. Disconnect cleanup cancels or pauses transient activity. These are tested foundations, not proof of every multi-client, restoration, or focus scenario.
- **Inspection and display tools.** The 62-tool catalog covers status/launch, bounded project scanning, exact database or migration-source selection, schema search/description, declared relation discovery, graph visibility/selection/field expansion/key focus/camera/layout, temporary arrangements and annotations, table filtering and sorting, bounded row and cell reads, record navigation, read-only SQL jobs/results, presentation control, preferences, saved explanations and selected exports. A migration set can be opened at an exact version; it exposes schema and graph views while row, query and row-export tools return `SCHEMA_ONLY_SOURCE`. Disconnected tables may be arranged nearby without asserting a relationship. Large query and export work is represented as cancellable jobs. MCP SQL remains read-only, including when the manually opened SQLite source is writable.
- **Bounded cell access and query jobs.** `studio_inspect_value` reads a database-side bounded slice of one selected cell and reports type, length, offset, and whether more content remains. `studio_run_query` returns a job handle promptly, with separate status, cancellation and bounded result retrieval. SQLite and PostgreSQL row, query and record reads enforce a hard materialization budget of 256 KiB per cell, 8 MiB per result page and 512 columns. Large individual values remain available through the bounded cell-inspection path. Native table grids omit oversized non-key cells, label them “Large value — inspect in slices,” and prevent editing them; the full value is not silently shown as complete. Result previews do not imply that a full result has been read or shown.
- **Safe native inspection of oversized cells.** The native table context menu offers “Inspect Cell Slice…” for oversized cells and disables full-record inspection when a row is incomplete. The inspector displays at most 4,096 units per slice and provides Previous/Next navigation. It checks the selected source and row context before paging so a changed selection cannot silently show a slice from a different cell. The focused native regression suite passed 4/4, including text and BLOB paging and stale-context cases.
- **Speech path.** Graph Studio owns captions, playback controls, view readiness and narration timing. A pinned Pocket TTS worker/provider and asset downloader stream generated audio; the final local universal app includes the Pocket runtime, which passed its packaged source check. The macOS fallback now writes callback audio to a private, unlinked temporary spool and feeds playback from a worker, so slow playback does not block the app's main thread or impose an eight-chunk burst limit. On the tested arm64 machine, the Pocket worker took about 23 seconds to start and produced its first PCM 46 ms after a synthesis request. A 3.12-second clip streamed in 0.785 seconds, with about 906 MiB peak worker RSS. This is a development-machine observation, not an 8 GB target-device result or a clean-install benchmark.
- **Preferences, artifacts, skills, and migration.** Preference tools and metadata migration exist; saved explanations preserve historical bounded evidence and can be reopened separately from refresh. The setup sheet defaults to user-wide setup and allows a reviewed project-folder option. Its read-only preflight shows client CLI versions and planned MCP/skill changes; install runs only after the user approves the preview. Project skills stay under the selected folder, Claude Code writes its project `.mcp.json` through its CLI, and Codex uses the project's `.codex/config.toml`; symlinked config or skill destinations are refused. After configuration read-back, setup launches the exact bundled helper to check MCP initialization, discover tools, and call read-only `studio_status`, reporting whether the app bridge is actually connected. This verifies the helper and its current app status, not that an already-running agent session has reloaded the connection. Generated skill resources retain the graph-clusters, schema-descriptions, database-preview, and database-diff workflows. Migration tests exercise removal of legacy story metadata while retaining unrelated values. First-use preference discovery and adaptation still need native end-to-end validation.

## Verification recorded

- On 23 September, a disposable four-table SQLite model was used for a broad packaged-app MCP/UI pass. Every one of the **62 catalog tool names** was invoked through the bundled helper against the running app, including status, workspace lifecycle, graph/table/record views, SQL jobs and exports, schema artifacts, explanations, preferences, and speech management. A successful MCP response alone is not counted as visual proof: native accessibility and screenshots separately confirmed split panes, temporary graph arrangement, filtering/sorting, query output, schema comparison, unapplied preview, presentation controls, offline explanation, and the full record graph. Read-only/status and file-export tools were checked by response or generated artifact. The launch tool was called while the app was already running; this pass does not prove cold launch. The setup sheet was opened in the app and correctly reported the host's existing Codex entry, unavailable Claude CLI, and blocked Claude skill symlink without changing settings.
- The pass included deliberately awkward inputs and states: invalid pane names, incomplete or excessive camera coordinates, unsupported event filters, task/workspace ownership mismatch, filtered result export, large values and bounded slices, NULL/orphan rows, schema-only sources, a cancelled recursive query, stale metadata updates, and speech requested for a nonvisible workspace. It exposed silent no-op view arguments, an unadvertised difference between graph-edge and record-relationship IDs, a saved-note overlay that covered graph controls, and a macOS speech callback that could block the app's main thread. The view tools now reject invalid/no-op inputs, relationship discovery returns both IDs, saved notes collapse to a compact control, and the fallback speech callback spools audio away from playback backpressure. The MCP server validates the catalog's supported JSON Schema shapes before dispatch. Unknown top-level inputs remain allowed for compatibility; callers should use the published fields.

- The final isolated Swift Testing run passed **560 tests across 85 suites**, with **45 environment-gated tests skipped**, in `/private/tmp/sgs-tool-matrix-final-full-tests.log`. It used the installed Swift Testing runtime because the ordinary `swift test` path in this Command Line Tools installation cannot find `XCTest.swiftmodule`; XCTest-only targets/tests therefore are not included. The run includes migration replay/restoration, project scanning through MCP, schema-only tool gates, stale query export, filtered group geometry, and the new speech/coordinator edge tests. The new MCP protocol XCTest cases are present in source but cannot run in this environment; the packaged helper's real JSON-RPC boundary was separately probed with fourteen malformed cases and a valid `studio_status` call (`/private/tmp/sgs-tool-matrix-final-schema-probe.log`).
- A post-merge local PostgreSQL 17 lane passed **16 tests across 4 suites**, with one unrelated owned-record fixture skipped, using an isolated database that was removed afterward (`/private/tmp/sgs-merge-postgres-fixture-tests.log`). Before the mainline merge, a broader owned-fixture PostgreSQL 17.11 lane passed **45 tests across 10 suites with zero skips**. The counts are separate runs.
- The setup installer’s read-only preflight and symlink refusal passed a focused isolated test **1/1**. The native oversized-cell slice inspector passed its focused regression suite **4/4**, covering text/BLOB paging and stale-context guards.
- The packaging Python suite passed **33 tests** again after the `0.4.0` metadata update (`/private/tmp/sgs-release-packaging-tests.log`); the separate preference migration check passed earlier. The bundled `StudioMCP` helper completed real stdio initialization, discovered **62 tools**, and connected to the exact packaged app. Its disposable-fixture smoke read projected rows near offset 999,990 in a million-row table, ran and cancelled query jobs, displayed the million-row count, read a bounded slice of a 16 MiB BLOB and a 150,000-character text cell, and round-tripped a durable note with stale-write rejection (`/private/tmp/sgs-final-mcp-smoke.log`). A separate packaged smoke scanned a project, opened an exact migration version, showed one schema table, and received `SCHEMA_ONLY_SOURCE` for a row request (`/private/tmp/sgs-final-migration-mcp-smoke.log`). Native accessibility and screenshot inspection confirmed the default split view, large-cell labels, migration step controls, and a schema-only column list.
- The rebuilt `0.4.0` / build `3` local app and helper contain both arm64 and x86_64 binaries; the packaged Pocket runtime passed its manifest, lock, architecture, and relocation checks. `hdiutil verify` passed for the **925 MiB** app and **377 MiB** DMG. The final DMG SHA-256 is `0626c94d0bdd7545e7d48494846f671bd47c4dafb9e2b3036e6ec206cc56ea2c`. This artifact is unsigned and unnotarized, so it is a local validation build, not a public download.
- The final packaged app completed a short and a longer macOS speech sample, accepted graph-card navigation while speaking, cancelled an actively speaking sample in **0.007 seconds** at the MCP job boundary, and completed a new sample afterward (`/private/tmp/sgs-tool-matrix-final-audio-probe.log`, `/private/tmp/sgs-tool-matrix-final-audio-interrupt.log`). Accessibility inspection confirmed the app remained responsive. This proves job/UI behavior for the local macOS fallback on this machine; it does not measure audible quality or the Pocket model inside the app.
- A later explicit `studio_manage_speech_assets` install in the running packaged app downloaded all four pinned Pocket files, completed the job, and matched their expected SHA-256 hashes. The first Pocket playback attempt exposed a short-ready-message pipe stall: the worker reached its input wait while Foundation's requested-size read kept the app waiting. The app now reads available bytes from the pipe; the focused short-message regression passed **1/1** in the isolated Swift Testing harness (`/private/tmp/sgs-release-short-ready-test-run.log`). In the rebuilt app, an Alba speech job advanced through generating, speaking, and completed in **10.149 seconds** on this warm development machine (`/private/tmp/sgs-release-pocket-audio-probe.log`); a second run cancelled active Pocket speech in **0.012 seconds** at the MCP job boundary and completed a fresh sample (`/private/tmp/sgs-release-pocket-audio-interrupt.log`). A live presentation reported the Pocket provider while speaking, and accessibility showed its caption and playback controls (`/private/tmp/sgs-release-pocket-presentation.log`). Native Pause changed that overlay to Paused, Continue returned it to Speaking, and End removed it during a second Pocket presentation (`/private/tmp/sgs-release-pocket-ui-pause.log`). After the final catalog repackage, the exact `0.4.0` bundle again completed an Alba sample (`/private/tmp/sgs-release-final-bundle-pocket-smoke.log`). These observations do not establish perceived voice quality, first-run timing on a clean machine, or 8 GB target performance.
- A native workspace-owner handoff regression check passed **1/1**. This is targeted evidence; the complete multi-client acceptance matrix remains unrun.

## Known product and release limits

- **No synchronized Codex Voice presentation.** The local MCP can accompany a Codex conversation with Graph Studio views, and Graph Studio can narrate its own points. The bridge does not receive Codex microphone, generated-audio timing, or interruption events, so synchronized speech and graph timing, including reliable user barge-in across both products, is not implemented or demonstrated. Treat them as separate conversation and presentation paths.
- **No Claude Code live result yet.** The setup preflight reports that Claude Code is unavailable on this host. Installer and skill-output behavior are covered by tests, but a real Claude Code installation and its protocol behavior have not been checked.
- **Speech distribution and target device are open.** One in-app explicit download, verified install, Pocket playback, live presentation, and MCP interruption/restart succeeded on this development Mac. That does not establish clean-machine install, perceived voice quality, long-session memory, native user interruption in every presentation state, or acceptable performance on an 8 GB Apple Silicon Mac. Intel builds use the macOS speech fallback because the pinned PyTorch runtime does not support the Intel target used here.
- **Some tool behavior is narrower than the design.** Current limits include single-column sorting, offset-based paging, no SQL-parameter interface, no general historical navigation beyond explicit captured checkpoints, no presentation speed control, and limited history pagination. MCP responses have a 1 MiB serialized-response cap; SQLite and PostgreSQL row/query/record materialization enforce separate 256 KiB per-cell, 8 MiB per-page and 512-column hard limits. Native grids omit oversized non-key values and mark them for slice inspection. Check tool schemas and result limits before claiming broader contracts.
- **Saved explanation and export scope is bounded.** Historical replay restores captured graph/layout/table selections and shows the captured table/query pages referenced by the current point. It reads only the offline artifact; pages with no point reference remain visible in the manual historical view. MCP export supports bounded CSV/JSON rows and transcripts; graph images and a packaged explanation export are not implemented through `studio_export`.
- **Interaction/accessibility coverage remains incomplete.** Keyboard and screen-reader paths, reduced motion, manual graph manipulation during narration, concurrent task/tab focus, and all failure/recovery paths need native testing. Multi-million-row paging and many-table scenarios have not yet been combined with simultaneous tabs, client activity, and speech in a release build.
- **Distribution signing is unavailable here.** The final local app and DMG are unsigned and unnotarized because this host has no Developer ID signing identity. Signing, notarization, clean installation on a separate Mac, and public distribution therefore remain external release steps.
- **PostgreSQL dump runtime packaging is open.** The current package does not include a self-contained redistributable PostgreSQL restore/runtime bundle. That optional capability requires a separately prepared universal relocatable runtime and release validation.

## Release assessment

The worktree has a working agent-directed MCP, graph/data workspace, bounded inspection/query paths, local narration, and a verified universal local build. This report does **not** certify public-release readiness. Remaining gates are Developer ID signing and notarization, clean installation on a separate Mac, measurement on an 8 GB Apple Silicon Mac, real Claude Code use, PostgreSQL dump runtime packaging if that capability is part of the release, and complete native accessibility, interruption, and multi-client acceptance scenarios. Codex Voice synchronization remains unsupported: Graph Studio narration is available, while shared audio timing and cross-product interruption are not implemented or validated.

For the complete expected scenario set, target-device measurements and validation method, see the [acceptance plan](agent-exploration-validation.md).
